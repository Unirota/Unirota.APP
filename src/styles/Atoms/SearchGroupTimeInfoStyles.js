import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
    marginTop: -5
  },
  timeText: {
    fontSize: 16,
    color: '#333',
    marginLeft: 5,
    fontWeight: '500'
  },
});

export default styles;