import { StyleSheet } from "react-native";

export default StyleSheet.create({
    groupContainer: {
        marginRight:75,
        flexDirection: 'row',
        justifyContent: 'space-evenly',
        alignItems: 'center'
    },
    font: {
        fontSize: 12
    }
})