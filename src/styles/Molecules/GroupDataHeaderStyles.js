import { StyleSheet } from 'react-native'

export const GroupDataStyles = StyleSheet.create({
    groupContainer: {
        marginRight:18,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    },
    font: {
        fontSize: 14,
    }
})